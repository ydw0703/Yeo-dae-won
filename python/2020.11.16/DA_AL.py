print(strtest[0],strtest[1])
#, 는 사이를 한 칸 띄워라.
print("ISE" in(not in) strtest) #boolean형으로 출력가능.
#data structure에 자료의 유무 판단시 매우 유용.
'''
Python은 switch문이 필요없다.
'''
continue => for loop안에 입력값을 초기화 하여 다시 for문으로 돌아가라
break =>  for loop을 빠져나가라.

함수에서 return값은 순서대로 정의된다.end=
y1,y2=f(x)에서 3,4 이면 y1=3, y2=4 처럼 입력이 차례대로 대입된다.end=

definition(def) in python has no type

lambdaadd=lambda numparam1, numparam2: numparam1,numparam2 #:뒤는 return값

x=[1,2,3]
y=[100,x,120]
#y요소인 x는 y에 저장되는 것이 아니라, 참조값이 저장되는 것이다.

"==" 그 위치에 값(Argument)이 동등하느냐의 판별.
"is" 그 위치가 자체가 동등한 참조(reference=저장장소)를 가지고 있는가를 판별.